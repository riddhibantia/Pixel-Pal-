<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- OS-level backstop: cleartext technically permitted so self-hosted
         agents on private LAN can pair (Android domain-config cannot express
         IP ranges). The REAL gate is GenericHttpAgentConnector.isAllowedEndpoint:
         HTTPS everywhere, cleartext only for loopback + RFC1918 private LAN.
         All first-party traffic (Firebase, Gemini) is HTTPS regardless. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
