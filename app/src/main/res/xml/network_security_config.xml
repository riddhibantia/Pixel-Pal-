<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext is only allowed for loopback/local-network agent endpoints.
         All first-party traffic (Firebase, Gemini) is HTTPS and pinned by default.
         GenericHttpAgentConnector enforces HTTPS; this config is the OS-level backstop. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <!-- Common LAN ranges for user-hosted agent (e.g. 192.168.x.x) -->
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>
</network-security-config>
